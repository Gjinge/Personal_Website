# Hello World — selected source snapshot

Coursework / learning prototype from Jinge Guo's local Unity project.
Original project files are preserved byte-for-byte in this snapshot. This is not a complete, importable Unity project: textures, audio, fonts, package dependencies, caches, editor tooling, and third-party sample content are not bundled. Restore the original assets and dependencies to run the complete project. This snapshot is for reading the code and scene structure.

See `ProjectSettings/ProjectVersion.txt` for the original editor version. Existing source comments are retained; tutorial-derived patterns are not claimed as original algorithms. No new license is assigned to third-party material.
